# lib/ — DOX

## Purpose
Utility server-side & shared — murni fungsi, tanpa React.

## Ownership — 13 modul

| Modul | File | Fungsi | Dipakai di |
|-------|------|--------|------------|
| **ruangKendali** | `ruangKendali.js` | (22 Sep 2026) Turunan murni untuk Ruang Kendali: `ringkasIndikator(pemdi)` (level dicapai/target per indikator), `antreanButir(pemdi)` (butir revisi+gap berprioritas), `bebanPJ(antrean, opd)`, `antreanUntukOPD`, `kunciPJ`, `kodePortal`, `hariKe`, `warnaAspek`, `PRIORITAS_URUT`. Tes: `test/ruangKendali.test.mjs` | `rkData.js`, rk/* |
| **rkData** | `rkData.js` | `susunDataRK()` (async) — merakit payload dashboard dari `pemdi.json`, `opd.json`, `linimasa.json`, `evaluasi-asesor-2026.json` (Patch 14: `situasi.asesor{indeks 1,24, mandiriAwal, level, aspek[], tanpaBukti, indeksLain, disetujui}` dan `indikator[].asesor{kode, nilai, level, verifikasi}`); bila `DATABASE_URL` ada, menerapkan overlay CMS (`lib/overlay.terapkanOverlay`) dan konten (`marquee`, `pengumuman`, `tenggat`); gagal DB → log + fallback JSON. `susunDataRKStatis()` versi sinkron murni | pages rk, `/api/rk-data`, `/admin` |
| **overlay** | `overlay.js` | (Patch 4) murni: `STATUS_SAH`, `PRIORITAS_SAH`, `KONTEN_SAH`, `validasiPatchButir`, `validasiKonten`, `terapkanOverlay(pemdi, baris)` (hanya status + catatan_mandiri.{ringkas,pj,prioritas,kebutuhan}; nama butir tak tersentuh; tandai `sumber:'cms'`), `susunEkspor(catatanMandiri, baris)`. Tes `test/overlay.test.mjs` | rkData, api/admin |
| **cetak** | `cetak.js` | (Patch 8, klien) `bukaCetak(html)` — buka jendela cetak/PDF dengan benar (`window.open` **tanpa** fitur `noopener`, yang selalu mengembalikan null), fallback iframe tersembunyi + `print()` bila pop-up diblokir. Dipakai Drawer & CatatanMandiri | rk/Drawer, asesor/CatatanMandiri |
| **revalidate** | `revalidate.js` | (Patch 7) `segarkanHalaman(res)` — `res.revalidate` /dashboard /indikator /antrean /admin setelah suntingan CMS; hasil `{ok,gagal}` dikembalikan di respons API (tidak ditelan) | api/admin |
| **db** | `db.js` | (Patch 4, server-only) Neon `@neondatabase/serverless` — atau **PGlite** (Postgres WASM, devDependency) bila `CMS_DB_LOKAL=<dir>` untuk uji lokal tanpa Neon (jangan di Vercel): `dbAktif`, `getSql`, `pastikanSkema` (CREATE IF NOT EXISTS), `bacaOverlay`, `simpanButir`, `simpanKonten`, `catatLog`, `bacaLog` | rkData, api/admin |
| **cmsAuth** | `cmsAuth.js` | (Patch 4) `cmsAktif`, `cekSandi` (timingSafeEqual), `buatToken`/`verifikasiToken` (HMAC-SHA256, exp 12 jam), cookie `pemdi_cms`, `wajibSesi(req,res,{peran})`, `labelSesi` | api/auth, api/admin |
| **pemdiNilai** | `pemdiNilai.js` | Rumus resmi PermenPANRB 8/2026 — `nilaiIndikator` (level kontinu semua-butir-**diterima**, aturan berjenjang), `indeksAspek` Σ(wI×NI)/wA, `indeksPemdi` Σ(wA×IA), `predikatPemdi` (Tabel 4), `statistikBukti`/`statistikIndikator` (5 status), `STATUS_META`/`STATUS_BUKTI`/`statusMeta` (vokabuler `diterima·revisi·proses·draf·belum` — satu sumber untuk semua halaman), konstanta LEVEL/INDIKATOR_EKSTERNAL; **`fokusLevel(ind)`** (21 Sep 2026: `levelDicapai` kumulatif-asesor = `nilaiIndikator`, `levelBerikut`, `tampil` = himpunan level terbuka default, `eksternal`) & **`peranLevel(level, fokus)`** (dicapai/berikut/lewat/nanti) — satu sumber fitur fokus level di /pemdi, /modul-indikator, beranda | `pemdi.js`, `modul-indikator.js`, `requirement.js`, `test/pemdiNilai.test.mjs` |
| **catatanMandiri** | `catatanMandiri.js` | (21 Sep 2026) Util Catatan Mandiri per butir: `butirBercatatan(ind)`, `teksCatatanButir(b, ind)`/`teksCatatanIndikator(ind)` (teks polos utk kolom catatan eval.spbe.go.id — memuat kode `I#-L#-##`, catatan asesor, rujukan bernomor + tautan), `htmlCatatanIndikator` (dokumen HTML mandiri → cetak/PDF lewat dialog peramban), `docxCatatanIndikator` (Blob **.docx OOXML minimal** dibangun sendiri — `zipStore` ZIP metode store + CRC32, tanpa pustaka; terverifikasi python-docx), `namaBerkasCatatan`, `PRIORITAS_META`/`JENIS_META`. Hanya dipanggil di klien (Blob/TextEncoder) | `components/asesor/CatatanMandiri.js`, `test/catatanMandiri.test.mjs` |
| **pjButir** | `pjButir.js` | (22 Sep 2026) `kumpulkanPJ(pemdi)` → string PJ tiap butir bercatatan mandiri; `hitungButirOPD(opd, pjList)` mencocokkan singkatan/nama OPD + tabel alias (Diskominfo ⇐ APTIKA/walidata/persandian, Setda ⇐ Bag. Organisasi/Hukum/Sekretariat Tim Koordinasi, dst.); `petaButirOPD(daftar, pemdi)` → peta `id → jumlah`. Server-side (getStaticProps). Pengganti hitungan "layanan per OPD" yang datanya sudah dihapus | `pages/opd/index.js`, `pages/index.js` (BerandaAsesor), `test/pjButir.test.mjs` |
| **search-index** | `search-index.js` | Pembangun korpus Fuse.js — indikator Pemdi, modul, OPD, glosarium, dokumen kunci (tanpa layanan/FAQ/SKM sejak 22 Sep 2026); dipanggil di getStaticProps | `cari.js` |
| **format** | `format.js` | `formatAngka`, `formatDesimal`, `gabung` — locale id-ID | lintas halaman |
| **slugify** | `slugify.js` | Slug URL konsisten | `opd/[slug].js`, `search-index.js` |
| **modeSitus** | `modeSitus.js` | (22 Sep 2026) Konstanta mode situs: `MODE_SITUS = "internal"`, `NOINDEX`. CommonJS (dipakai `middleware.js` Edge, `_document`, `next-sitemap.config.js`, `api/health`). Saklar `NEXT_PUBLIC_PERSONA_PUBLIK` **sudah tidak ada** | `middleware.js`, `_document.js`, `api/health.js`, `next-sitemap.config.js` |

> **Dihapus 22 Sep 2026** (arsip tag `arsip/persona-publik-2026-09`): `security.js`, `sanitize.js`, `rate-limit-db.js`, `adminAuth.js`, `supabaseAdmin.js`, `persona.js`, `sektorLayanan.js` beserta `db/*.sql` dan `test/persona.test.mjs`. Dependensi `@supabase/supabase-js` dilepas dari `package.json`.

## Key Rules
- Perubahan `pemdiNilai.js` wajib `npm test` hijau (pin rumus + fokusLevel + KONSISTENSI lintas-data pemdi⇔modul⇔draf: indeks simulasi 0,35 · proyeksi 2,29 · 232 bukti = 18 diterima/19 revisi/0 proses/12 draf/183 belum) — total suite **55 tes** (pemdiNilai · requirement · catatanMandiri · pjButir · cekUi · overlay · ruangKendali)
- `pjButir.js`: bila menambah OPD/PJ baru di `catatan-mandiri.json`, pastikan singkatan ada di `data/opd.json` atau tambahkan alias di `ALIAS`; jalankan `node --test test/pjButir.test.mjs`
- Modul yang dipakai middleware Edge (`modeSitus.js`) harus tetap CommonJS tanpa import Node
